# Copia este archivo a docker.env y ajusta los valores.
# Luego: docker run --env-file docker.env -p 8000:8000 calculadora-fantasy-api

# URL del frontend (donde está la web). Obligatoria para CORS.
FRONTEND_URL=https://www.calculadorafantasy.com

# Puerto dentro del contenedor (opcional, por defecto 8000)
# PORT=8000
